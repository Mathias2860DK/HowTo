package Interfaces;

public interface Login {
    /*
    This interfaces have one method. The login-method. Every class that implements this interface must have
    the login-method
     */

    void login();
}
