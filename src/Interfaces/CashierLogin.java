package Interfaces;

public class CashierLogin implements Login{
    //The CasiherLogin implements the Login interface, which means it should have the method inside.
    @Override
    public void login() {
        //How does the Cashier log in?
    }
}
